<header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Les potits chats</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
              <li class="nav-item">
                <a class="nav-link active" href="<%=items_path%>">Home
                  <span class="visually-hidden">(current)</span>
                </a>
              </li>
               <li>
                <%= link_to "Panier", cart_show_path, class: "btn btn-primary", data: {"turbo-frame"=> "cart"} %>
                </li>
              <li class="nav-item">
                <a class="nav-link" href="#">About</a>
              </li>
            </ul>
            <ul class="navbar-nav float-end">
            <%= button_to "Passer commande", checkout_create_path(total: 10), class: "btn btn-primary", "data-turbo" => "false" %>
              <% if user_signed_in? %>
                <li>
                  <%= button_to "Sign out", destroy_user_session_path, class:"btn btn-secondary", method: :delete %>
                </li>
              <% else %>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Connexion</a>
                  <div class="dropdown-menu">
                    <%= link_to "SignUp", new_user_registration_path, class:"dropdown-item" %>
                    <%= link_to "LogIn", new_user_session_path, class:"dropdown-item" %>
                  </div>
                </li>
                <li>
                <%= link_to "Panier", cart_show_path, class: "btn btn-primary", data: {"turbo-frame"=> "cart"} %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </nav>
  </header>